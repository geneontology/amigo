#!/usr/bin/perl -w
####
#### A GOlr/BBOP JS REPL bootstrap.
####

BEGIN { require "config.pl"; }
use lib $ENV{AMIGO_ROOT} . '/perl/lib';

use AmiGO::WebApp::REPL;
my $repl = AmiGO::WebApp::REPL->new(PARAMS => {});
$repl->run();
