<?xml version='1.0' encoding='UTF-8'?>
<schema name="golr" version="3.6">
  <types>
    <!--Unsplit string for when text needs to be dealt with atomically.-->
    <!--For example, faceted querying.-->
    <fieldType name="string" class="solr.StrField" sortMissingLast="true"/>
    <!--Any string with spaces that needs to be treated for searching purposes.-->
    <!--This will be automatically used in cases where "searchable: true" has been-->
    <!--specified in the YAML.-->
    <fieldType name="text_searchable" class="solr.TextField" positionIncrementGap="100" sortMissingLast="true">
      <analyzer>
        <tokenizer class="solr.StandardTokenizerFactory"/>
        <filter class="solr.LowerCaseFilterFactory"/>
      </analyzer>
    </fieldType>
    <fieldType name="integer" class="solr.TrieIntField" precisionStep="0" positionIncrementGap="0" sortMissingLast="true"/>
    <fieldType name="boolean" class="solr.BoolField" sortMissingLast="true"/>
  </types>
  <fields>
    <!-- A special static/fixed (by YAML conf file) field all documents have. -->
    <field name="document_category" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Term identifier. -->
    <!-- bioentity: Gene of gene product ID. -->
    <!-- annotation: A unique (and internal) combination of bioentity and ontology class. -->
    <!-- general: The mangled internal ID for this entity. -->
    <!-- model_annotation: CURIE form of annoton (molecular function individual) -->
    <!-- noctua_model_meta: The mangled internal ID for this entity. -->
    <!-- family: Family ID. -->
    <!-- bbop_ann_ev_agg: Gene/product ID. -->
    <field name="id" type="string" required="true" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Term identifier. -->
    <!-- annotation: Direct annotations. -->
    <!-- bbop_ann_ev_agg: Column 5. -->
    <field name="annotation_class" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Identifier. -->
    <!-- annotation: Direct annotations. -->
    <!-- bbop_ann_ev_agg: Column 5 + ontology. -->
    <field name="annotation_class_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of annotation_class_label.-->
    <field name="annotation_class_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="annotation_class_label" dest="annotation_class_label_searchable"/>
    <!-- ontology: Term definition. -->
    <field name="description" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of description.-->
    <field name="description_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="description" dest="description_searchable"/>
    <!-- ontology: Term namespace. In GO, this is one of: biological_process, molecular_function, cellular_component. In other ontologies it is not guaranteed to be populated. Consider using idspace instead for general queries. -->
    <!-- bioentity: Database source. -->
    <!-- bioentity_for_browser: Database source. -->
    <!-- annotation: Database source. -->
    <field name="source" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Term ID space. -->
    <field name="idspace" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Is the term obsolete? -->
    <field name="is_obsolete" type="boolean" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Term comments. -->
    <!-- model_annotation: Comments -->
    <!-- noctua_model_meta: The comments associated with a model. -->
    <field name="comment" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of comment.-->
    <field name="comment_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="comment" dest="comment_searchable"/>
    <!-- ontology: Term synonyms. -->
    <!-- bioentity: Gene product synonyms. -->
    <!-- annotation: Gene or gene product synonyms. -->
    <field name="synonym" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of synonym.-->
    <field name="synonym_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="synonym" dest="synonym_searchable"/>
    <!-- ontology: Alternate term identifier. -->
    <field name="alternate_id" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Term that replaces this term. -->
    <field name="replaced_by" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Others terms you might want to look at. -->
    <field name="consider" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Special use collections of terms. -->
    <field name="subset" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Definition cross-reference. -->
    <field name="definition_xref" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Database cross-reference. -->
    <!-- bioentity: Database cross-reference. -->
    <field name="database_xref" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Ancestral terms (is_a/part_of). -->
    <!-- bioentity: Closure of ids/accs over isa and partof. -->
    <!-- annotation: Annotations for this term or its children (over is_a/part_of). -->
    <field name="isa_partof_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Ancestral terms (is_a/part_of). -->
    <!-- bioentity: Closure of labels over isa and partof. -->
    <!-- annotation: Annotations for this term or its children (over is_a/part_of). -->
    <field name="isa_partof_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of isa_partof_closure_label.-->
    <field name="isa_partof_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="isa_partof_closure_label" dest="isa_partof_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between isa_partof_(list|closure) and isa_partof_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="isa_partof_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: Ancestral terms (is_a). -->
    <field name="isa_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Ancestral terms (is_a) labels. -->
    <field name="isa_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of isa_closure_label.-->
    <field name="isa_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="isa_closure_label" dest="isa_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between isa_(list|closure) and isa_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="isa_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: Ancestral terms (regulates, occurs in, capable_of). -->
    <!-- bioentity: Bioentities associated with this term or its children (over regulates). -->
    <!-- annotation: Annotations for this term or its children (over regulates). -->
    <field name="regulates_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Ancestral terms (regulates, occurs in, capable_of). -->
    <!-- bioentity: Bioentities associated with this term or its children (over regulates). -->
    <!-- annotation: Annotations for this term or its children (over regulates). -->
    <field name="regulates_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of regulates_closure_label.-->
    <field name="regulates_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="regulates_closure_label" dest="regulates_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between regulates_(list|closure) and regulates_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="regulates_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: JSON blob form of the local stepwise topology graph. Uses various relations (including regulates, occurs in, capable_of). -->
    <!-- model_annotation: JSON blob form of the local stepwise topology graph. -->
    <field name="topology_graph_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: JSON blob form of the local relation transitivity graph. Uses various relations (including regulates, occurs in, capable_of). -->
    <field name="regulates_transitivity_graph_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: JSON blob form of the local relation transitivity graph. -->
    <field name="isa_partof_transitivity_graph_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: JSON blob form of all immediate neighbors of the term. -->
    <field name="neighborhood_graph_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: JSON blob form of all immediate neighbors of the term; in the case that there are too many neighbors to transport, the number will be artificially reduced. -->
    <field name="neighborhood_limited_graph_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: Only in taxon. -->
    <field name="only_in_taxon" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of only_in_taxon.-->
    <field name="only_in_taxon_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="only_in_taxon" dest="only_in_taxon_searchable"/>
    <!-- ontology: Only in taxon label. -->
    <field name="only_in_taxon_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of only_in_taxon_label.-->
    <field name="only_in_taxon_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="only_in_taxon_label" dest="only_in_taxon_label_searchable"/>
    <!-- ontology: Only in taxon closure. -->
    <field name="only_in_taxon_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Only in taxon label closure. -->
    <field name="only_in_taxon_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of only_in_taxon_closure_label.-->
    <field name="only_in_taxon_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="only_in_taxon_closure_label" dest="only_in_taxon_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between only_in_taxon_(list|closure) and only_in_taxon_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="only_in_taxon_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- ontology: A non-lossy representation of conjunctions and disjunctions in c16 (JSON). -->
    <field name="annotation_extension_owl_json" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: This is equivalent to the relation field in GPAD. -->
    <field name="annotation_relation" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: This is equivalent to the relation field in GPAD. -->
    <field name="annotation_relation_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of annotation_relation_label.-->
    <field name="annotation_relation_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="annotation_relation_label" dest="annotation_relation_label_searchable"/>
    <!-- ontology: For any class document C, this will contain json(CE) for all axioms of form EquivalentClasses(C ... CE ....). -->
    <field name="equivalent_class_expressions_json" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- ontology: Disjoint classes. -->
    <field name="disjoint_class_list" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- ontology: Disjoint classes. -->
    <field name="disjoint_class_list_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of disjoint_class_list_label.-->
    <field name="disjoint_class_list_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="disjoint_class_list_label" dest="disjoint_class_list_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between disjoint_class_(list|closure) and disjoint_class_list_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="disjoint_class_list_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- bioentity: Gene or gene product identifiers. -->
    <!-- annotation: Gene or gene product identifiers. -->
    <!-- bbop_ann_ev_agg: Column 1 + columns 2. -->
    <field name="bioentity" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- bioentity: Gene or gene product identifiers. -->
    <!-- annotation: Gene or gene product identifiers. -->
    <!-- bbop_ann_ev_agg: Column 3. -->
    <field name="bioentity_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of bioentity_label.-->
    <field name="bioentity_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="bioentity_label" dest="bioentity_label_searchable"/>
    <!-- bioentity: The full name of the gene or gene product. -->
    <!-- annotation: The full name of the gene or gene product. -->
    <field name="bioentity_name" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of bioentity_name.-->
    <field name="bioentity_name_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="bioentity_name" dest="bioentity_name_searchable"/>
    <!-- bioentity: The bioentity ID used at the database of origin. -->
    <!-- annotation: The bioentity ID used at the database of origin. -->
    <field name="bioentity_internal_id" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- bioentity: Type class. -->
    <!-- bioentity_for_browser: Type class. -->
    <!-- annotation: Type class. -->
    <field name="type" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- bioentity: Taxonomic group -->
    <!-- bioentity_for_browser: Taxonomic group -->
    <!-- annotation: Taxonomic group. -->
    <!-- annotation_for_browser: Taxonomic group. -->
    <!-- model_annotation: taxon of the entity in enabled_by -->
    <!-- bbop_ann_ev_agg: Column 13: taxon. -->
    <field name="taxon" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- bioentity: Taxonomic group -->
    <!-- bioentity_for_browser: Taxonomic group -->
    <!-- annotation: Taxonomic group and ancestral groups. -->
    <!-- annotation_for_browser: Taxonomic group and ancestral groups. -->
    <!-- model_annotation: label for taxon -->
    <!-- bbop_ann_ev_agg: Derived from C13 + ncbi_taxonomy.obo. -->
    <field name="taxon_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of taxon_label.-->
    <field name="taxon_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="taxon_label" dest="taxon_label_searchable"/>
    <!-- bioentity: Taxonomic group and ancestral groups. -->
    <!-- bioentity_for_browser: Taxonomic group and ancestral groups. -->
    <!-- annotation: Taxonomic group and ancestral groups. -->
    <!-- annotation_for_browser: Taxonomic group and ancestral groups. -->
    <!-- model_annotation: is_a closure of taxon -->
    <!-- bbop_ann_ev_agg: IDs derived from C13 + ncbi_taxonomy.obo. -->
    <field name="taxon_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- bioentity: Taxonomic group and ancestral groups. -->
    <!-- bioentity_for_browser: Taxonomic group and ancestral groups. -->
    <!-- annotation: Taxonomic group and ancestral groups. -->
    <!-- annotation_for_browser: Taxonomic group and ancestral groups. -->
    <!-- model_annotation: labels of is_a closure. -->
    <!-- bbop_ann_ev_agg: Labels derived from C13 + ncbi_taxonomy.obo. -->
    <field name="taxon_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of taxon_closure_label.-->
    <field name="taxon_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="taxon_closure_label" dest="taxon_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between taxon_(list|closure) and taxon_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="taxon_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- bioentity: Taxonomic group (direct) and ancestral groups that are within the specified subset (e.g mammalia, eukaryota). -->
    <!-- bioentity_for_browser: Taxonomic group (direct) and ancestral groups that are within the specified subset (e.g mammalia, eukaryota). -->
    <!-- annotation: Taxonomic group (direct) and ancestral groups that are within the specified subset (e.g mammalia, eukaryota). -->
    <!-- annotation_for_browser: Taxonomic group (direct) and ancestral groups that are within the specified subset (e.g mammalia, eukaryota). -->
    <field name="taxon_subset_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- bioentity: Labels for taxonomic group (direct) and ancestral groups that are within the specified subset. -->
    <!-- bioentity_for_browser: Labels for taxonomic group (direct) and ancestral groups that are within the specified subset. -->
    <!-- annotation: Taxonomic group (direct) and ancestral groups that are within the specified subset (e.g mammalia, eukaryota). -->
    <!-- annotation_for_browser: Labels for taxonomic group (direct) and ancestral groups that are within the specified subset. -->
    <field name="taxon_subset_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of taxon_subset_closure_label.-->
    <field name="taxon_subset_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="taxon_subset_closure_label" dest="taxon_subset_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between taxon_subset_(list|closure) and taxon_subset_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="taxon_subset_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- bioentity: Direct annotations. -->
    <field name="annotation_class_list" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- bioentity: Direct annotations. -->
    <field name="annotation_class_list_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between annotation_class_(list|closure) and annotation_class_list_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="annotation_class_list_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- bioentity: PANTHER families that are associated with this entity. -->
    <!-- annotation: PANTHER families that are associated with this entity. -->
    <!-- model_annotation: PANTHER family IDs that are associated with this entity, not yet populated. -->
    <!-- family: PANTHER family IDs that are associated with this entity. -->
    <!-- bbop_ann_ev_agg: Family IDs that are associated with this entity. -->
    <field name="panther_family" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of panther_family.-->
    <field name="panther_family_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="panther_family" dest="panther_family_searchable"/>
    <!-- bioentity: PANTHER families that are associated with this entity. -->
    <!-- annotation: PANTHER families that are associated with this entity. -->
    <!-- model_annotation: PANTHER families that are associated with this entity. -->
    <!-- family: PANTHER families that are associated with this entity. -->
    <!-- bbop_ann_ev_agg: Families that are associated with this entity. -->
    <field name="panther_family_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of panther_family_label.-->
    <field name="panther_family_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="panther_family_label" dest="panther_family_label_searchable"/>
    <!-- bioentity: JSON blob form of the phylogenic tree. -->
    <!-- family: JSON blob form of the phylogenic tree. -->
    <field name="phylo_graph_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- annotation: Date of assignment. -->
    <field name="date" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Annotations assigned by group. -->
    <field name="assigned_by" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Rational for redundancy of annotation. -->
    <field name="is_redundant_for" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Secondary taxon. -->
    <field name="secondary_taxon" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Secondary taxon. -->
    <field name="secondary_taxon_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of secondary_taxon_label.-->
    <field name="secondary_taxon_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="secondary_taxon_label" dest="secondary_taxon_label_searchable"/>
    <!-- annotation: Secondary taxon closure. -->
    <field name="secondary_taxon_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Secondary taxon closure. -->
    <field name="secondary_taxon_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of secondary_taxon_closure_label.-->
    <field name="secondary_taxon_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="secondary_taxon_closure_label" dest="secondary_taxon_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between secondary_taxon_(list|closure) and secondary_taxon_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="secondary_taxon_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- annotation: Closure of ids/accs over has_participant. -->
    <field name="has_participant_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Closure of labels over has_participant. -->
    <field name="has_participant_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of has_participant_closure_label.-->
    <field name="has_participant_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="has_participant_closure_label" dest="has_participant_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between has_participant_(list|closure) and has_participant_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="has_participant_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- annotation: Annotation qualifier. -->
    <field name="qualifier" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Ontology aspect. -->
    <field name="aspect" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Biological isoform. -->
    <field name="bioentity_isoform" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Evidence type. (legacy) -->
    <!-- annotation_for_browser: Evidence type. -->
    <!-- model_annotation: Evidence type. -->
    <field name="evidence_type" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: All evidence (evidence closure) for this annotation. (legacy) -->
    <!-- annotation_for_browser: All evidence (evidence closure) for this annotation -->
    <!-- model_annotation: All evidence (evidence closure) for this annotation -->
    <!-- bbop_ann_ev_agg: All evidence for this term/gene product pair -->
    <field name="evidence_type_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Evidence with/from. -->
    <!-- model_annotation: Evidence with/from. -->
    <!-- bbop_ann_ev_agg: All column 8s for this term/gene product pair -->
    <field name="evidence_with" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of evidence_with.-->
    <field name="evidence_with_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="evidence_with" dest="evidence_with_searchable"/>
    <!-- annotation: Evidence. -->
    <field name="evidence" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- annotation: Evidence. -->
    <field name="evidence_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of evidence_label.-->
    <field name="evidence_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="evidence_label" dest="evidence_label_searchable"/>
    <!-- annotation: All evidence for this annotation. -->
    <field name="evidence_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: All evidence for this annotation. -->
    <field name="evidence_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of evidence_closure_label.-->
    <field name="evidence_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="evidence_closure_label" dest="evidence_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between evidence_(list|closure) and evidence_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="evidence_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- annotation: All evidence for this annotation reduced to a usable subset. -->
    <field name="evidence_subset_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: All evidence for this annotation reduced to a usable subset. -->
    <field name="evidence_subset_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of evidence_subset_closure_label.-->
    <field name="evidence_subset_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="evidence_subset_closure_label" dest="evidence_subset_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between evidence_subset_(list|closure) and evidence_subset_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="evidence_subset_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- annotation: Database reference. -->
    <!-- model_annotation: Database reference. -->
    <field name="reference" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of reference.-->
    <field name="reference_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="reference" dest="reference_searchable"/>
    <!-- annotation: Extension class for the annotation. -->
    <field name="annotation_extension_class" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Extension class for the annotation. -->
    <field name="annotation_extension_class_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of annotation_extension_class_label.-->
    <field name="annotation_extension_class_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="annotation_extension_class_label" dest="annotation_extension_class_label_searchable"/>
    <!-- annotation: Extension class for the annotation. -->
    <field name="annotation_extension_class_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Extension class for the annotation. -->
    <field name="annotation_extension_class_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of annotation_extension_class_closure_label.-->
    <field name="annotation_extension_class_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="annotation_extension_class_closure_label" dest="annotation_extension_class_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between annotation_extension_class_(list|closure) and annotation_extension_class_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="annotation_extension_class_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- annotation: Extension class for the annotation (JSON). -->
    <field name="annotation_extension_json" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Experimental numeric type (X). -->
    <field name="geospatial_x" type="integer" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Experimental numeric type (Y). -->
    <field name="geospatial_y" type="integer" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- annotation: Experimental numeric type (Z). -->
    <field name="geospatial_z" type="integer" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- general: The ID/label for this entity. -->
    <field name="entity" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- general: The label for this entity. -->
    <field name="entity_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of entity_label.-->
    <field name="entity_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="entity_label" dest="entity_label_searchable"/>
    <!-- general: The document category that this enitity belongs to. -->
    <field name="category" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- general: A hidden searchable blob document to access this item. It should contain all the goodies that we want to search for, like species(?), synonyms, etc. -->
    <field name="general_blob" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of general_blob.-->
    <field name="general_blob_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="general_blob" dest="general_blob_searchable"/>
    <!-- model_annotation: Same as id -->
    <!-- noctua_model_meta: The title(s) associated with the model. -->
    <field name="annotation_unit" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- model_annotation: Generally not display but for debug purpose can be concatenation of gene product label, function, process etc -->
    <!-- noctua_model_meta: The title(s) associated with the model. -->
    <field name="annotation_unit_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of annotation_unit_label.-->
    <field name="annotation_unit_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="annotation_unit_label" dest="annotation_unit_label_searchable"/>
    <!-- model_annotation: The CURIE of the model to which the annoton belongs -->
    <field name="model" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- model_annotation: The dc:title of the model to which the annoton belongs -->
    <field name="model_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of model_label.-->
    <field name="model_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="model_label" dest="model_label_searchable"/>
    <!-- model_annotation: The URI of the model to which the annoton belongs -->
    <field name="model_url" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- model_annotation: Curation status -->
    <!-- noctua_model_meta: The editorial state of the model. -->
    <field name="model_state" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- model_annotation: set of all literal values of all annotation assertions in model -->
    <field name="annotation_value" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- model_annotation: Value of dc:contributor fields for the annoton -->
    <!-- noctua_model_meta: Contributor identity. -->
    <field name="contributor" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of contributor.-->
    <field name="contributor_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="contributor" dest="contributor_searchable"/>
    <!-- model_annotation: Value of dc:date field for the annoton -->
    <!-- noctua_model_meta: Model last modification dates. -->
    <field name="model_date" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of model_date.-->
    <field name="model_date_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="model_date" dest="model_date_searchable"/>
    <!-- model_annotation: The CURIE for the ro:enabled_by.rdf:type value of the annoton/MF individual -->
    <field name="enabled_by" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of enabled_by.-->
    <field name="enabled_by_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="enabled_by" dest="enabled_by_searchable"/>
    <!-- model_annotation: Searchable label version of the gene product -->
    <field name="enabled_by_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of enabled_by_label.-->
    <field name="enabled_by_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="enabled_by_label" dest="enabled_by_label_searchable"/>
    <!-- model_annotation: This is the value of rdf:type for the annoton/MF instance -->
    <field name="function_class" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- model_annotation: Common function name. -->
    <field name="function_class_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of function_class_label.-->
    <field name="function_class_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="function_class_label" dest="function_class_label_searchable"/>
    <!-- model_annotation: ??? -->
    <field name="function_class_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- model_annotation: ??? -->
    <field name="function_class_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of function_class_closure_label.-->
    <field name="function_class_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="function_class_closure_label" dest="function_class_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between function_class_(list|closure) and function_class_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="function_class_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- model_annotation: Process acc/ID. -->
    <field name="process_class" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!-- model_annotation: Common process name. -->
    <field name="process_class_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of process_class_label.-->
    <field name="process_class_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="process_class_label" dest="process_class_label_searchable"/>
    <!-- model_annotation: ??? -->
    <field name="process_class_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- model_annotation: ??? -->
    <field name="process_class_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of process_class_closure_label.-->
    <field name="process_class_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="process_class_closure_label" dest="process_class_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between process_class_(list|closure) and process_class_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="process_class_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- model_annotation:  -->
    <field name="location_list" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- model_annotation:  -->
    <field name="location_list_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between location_(list|closure) and location_list_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="location_list_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- model_annotation:  -->
    <field name="location_list_closure" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- model_annotation:  -->
    <field name="location_list_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between location_list_(list|closure) and location_list_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="location_list_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- model_annotation: ??? -->
    <!-- noctua_model_meta: ??? -->
    <field name="owl_blob_json" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- model_annotation: Evidence type. -->
    <field name="evidence_type_label" type="string" required="false" multiValued="false" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of evidence_type_label.-->
    <field name="evidence_type_label_searchable" type="text_searchable" required="false" multiValued="false" indexed="true" stored="true"/>
    <copyField source="evidence_type_label" dest="evidence_type_label_searchable"/>
    <!-- model_annotation: All evidence (evidence closure) for this annotation -->
    <field name="evidence_type_closure_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!--An easily searchable (TextField tokenized) version of evidence_type_closure_label.-->
    <field name="evidence_type_closure_label_searchable" type="text_searchable" required="false" multiValued="true" indexed="true" stored="true"/>
    <copyField source="evidence_type_closure_label" dest="evidence_type_closure_label_searchable"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between evidence_type_(list|closure) and evidence_type_closure_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="evidence_type_closure_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
    <!-- family: Gene/products annotated with this protein family. -->
    <field name="bioentity_list" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- family: Gene/products annotated with this protein family. -->
    <field name="bioentity_list_label" type="string" required="false" multiValued="true" indexed="true" stored="true"/>
    <!-- Automatically created to capture mapping information -->
    <!-- between bioentity_(list|closure) and bioentity_list_label.-->
    <!-- It is not indexed for searching (JSON blob), but may be useful to the client. -->
    <field name="bioentity_list_map" type="string" required="false" multiValued="false" indexed="false" stored="true"/>
  </fields>
  <uniqueKey>id</uniqueKey>
</schema>
